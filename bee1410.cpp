#include <bits/stdc++.h>

using namespace std;

int main(){

    int a, b;
    cin >> a >> b;

    while(a || b){

        vector<int> at(a), df(b);

        for(int i = 0; i < a; i++) cin >> at[i];
        for(int i = 0; i < b; i++) cin >> df[i];

        sort(at.begin(), at.end());
        sort(df.begin(), df.end());

        bool flag = true;

        for(int i = 0; i < a; i++){
            if(at[i] == )
        }

        cin >> a >> b;
    }

    return 0;
}
