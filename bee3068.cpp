#include <bits/stdc++.h>

using namespace std;


int main(){

    int x1, y1, x2, y2;
    cin >> x1 >> y1 >> x2 >> y2;
    
    int n, idx = 1, t, x, y;

    while(x1 || y1 || x2 || y2){

        cin >> n;
        t = 0;
        for(int i = 0; i < n; i++){
            cin >> x >> y;

            if()

        }

        idx++;

        cin >> x1 >> y1 >> x2 >> y2;
    }

    return 0;
}
